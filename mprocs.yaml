procs:
  services:
    shell: docker compose up
  api:
    # api/ holds the .NET solution + Makefile after the repo split.
    # -C api makes Make change to that dir before parsing the Makefile.
    shell: sleep 15 && make -C api dev-api
